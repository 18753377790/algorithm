package com.algorithm.geekbang.basic.search;

/**
 * @Author 李非凡
 * @Description:
 * 二分查找
 * 二分查找针对的是一个有序的数据集合，查找思想有点类似分治思想。每次都通过跟区间的中
 * 间元素对比，将待查找的区间缩小为之前的一半，直到找到要查找的元素，或者区间被缩小为 0。
 *
 * 二分查找的变形问题：
 * 1.查找第一个值等于给定值的元素
 * 2.查找最后一个值等于给定值的元素
 * 3.查找第一个大于等于给定值的元素
 * 4.查找最后一个小于等于给定值的元素
 * @Date 2019/10/9 22:43
 * @Version 1.0
 */
public class BinarySearch {

    /**
     * 二分查找
     * @param a 待查找的数组
     * @param n 数组的长度
     * @param value 待查找的数值
     * @return -1为未找到
     */
    private int binarySearch(int[] a, int n, int value){
        return 0;
    }

    /**
     * 查找第一个值等于给定值的元素
     * @param a 待查找的数组
     * @param n 数组的长度
     * @param value 待查找的数值
     * @return -1为未找到
     */
    private int binarySearch1(int[] a, int n, int value){
        return 0;
    }

    /**
     * 查找最后一个值等于给定值的元素
     * @param a
     * @param n
     * @param value
     * @return
     */
    private int binarySearch2(int[] a, int n, int value){
        return 0;
    }

    /**
     * 查找第一个大于等于给定值的元素
     * @param a
     * @param n
     * @param value
     * @return
     */
    private int binarySearch3(int[] a, int n, int value){
        return 0;
    }

    /**
     * 查找最后一个小于等于给定值的元素
     * @param a
     * @param n
     * @param value
     * @return
     */
    private int binarySearch4(int[] a, int n, int value){
        return 0;
    }

    public static void main(String[] args) {

    }
}
